package shop

import (
	"github.com/gin-gonic/gin"
)

func Buy(ctx *gin.Context)  {

	//userId := ctx.MustGet("user_id").(int64)
	//num, _ := strconv.ParseInt(ctx.DefaultPostForm("num","0"), 10, 64)
	//goods_spu_id,_ := strconv.ParseInt(ctx.PostForm("goods_spu_id"), 10, 64)
	//address_id,_ := strconv.ParseInt(ctx.PostForm("address_id"), 10, 64)
	//coupon_id,_ := strconv.ParseInt(ctx.PostForm("coupon_id"), 10, 64)
















}
